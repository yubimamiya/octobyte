import { LanguageModelV4, ProviderV4, Experimental_BatchV4, EmbeddingModelV4, ImageModelV4, Experimental_VideoModelV4, RerankingModelV4, Experimental_EvaluationModelV4, SpeechModelV4, TranscriptionModelV4, Experimental_RealtimeFactoryV4, JSONValue, TypeValidationError } from '@ai-sdk/provider';
import * as _ai_sdk_provider_utils from '@ai-sdk/provider-utils';
import { FetchFunction, WebSocketConstructor, InferSchema } from '@ai-sdk/provider-utils';

type GatewayEmbeddingModelId = 'alibaba/qwen3-embedding-0.6b' | 'alibaba/qwen3-embedding-4b' | 'alibaba/qwen3-embedding-8b' | 'amazon/titan-embed-text-v2' | 'cohere/embed-v4.0' | 'google/gemini-embedding-001' | 'google/gemini-embedding-2' | 'google/text-embedding-005' | 'google/text-multilingual-embedding-002' | 'mistral/codestral-embed' | 'mistral/mistral-embed' | 'openai/text-embedding-3-large' | 'openai/text-embedding-3-small' | 'openai/text-embedding-ada-002' | 'perplexity/pplx-embed-v1-0.6b' | 'perplexity/pplx-embed-v1-4b' | 'voyage/voyage-3-large' | 'voyage/voyage-3.5' | 'voyage/voyage-3.5-lite' | 'voyage/voyage-4' | 'voyage/voyage-4-large' | 'voyage/voyage-4-lite' | 'voyage/voyage-code-2' | 'voyage/voyage-code-3' | 'voyage/voyage-finance-2' | 'voyage/voyage-law-2' | (string & {});

type GatewayEvaluationModelId = 'typesafe-ai/jev' | (string & {});

type GatewayImageModelId = 'bfl/flux-2-flex' | 'bfl/flux-2-klein-4b' | 'bfl/flux-2-klein-9b' | 'bfl/flux-2-max' | 'bfl/flux-2-pro' | 'bfl/flux-kontext-max' | 'bfl/flux-kontext-pro' | 'bfl/flux-pro-1.0-fill' | 'bfl/flux-pro-1.1' | 'bfl/flux-pro-1.1-ultra' | 'bytedance/seedream-4.0' | 'bytedance/seedream-4.5' | 'bytedance/seedream-5.0-lite' | 'bytedance/seedream-5.0-pro' | 'meta/muse-image-1.0' | 'openai/gpt-image-1' | 'openai/gpt-image-1-mini' | 'openai/gpt-image-1.5' | 'openai/gpt-image-2' | 'openai/gpt-image-2.5-flare' | 'openai/gpt-image-2.5-sunburst' | 'prodia/flux-fast-schnell' | 'quiverai/arrow-1.1' | 'recraft/recraft-v2' | 'recraft/recraft-v3' | 'recraft/recraft-v4' | 'recraft/recraft-v4-pro' | 'recraft/recraft-v4.1' | 'recraft/recraft-v4.1-pro' | 'recraft/recraft-v4.1-utility' | 'recraft/recraft-v4.1-utility-pro' | 'spacexai/grok-imagine-image' | 'spacexai/grok-imagine-image-2.0' | (string & {});

type GatewayModelId = 'alibaba/qwen-3-14b' | 'alibaba/qwen-3-235b' | 'alibaba/qwen-3-30b' | 'alibaba/qwen-3-32b' | 'alibaba/qwen-3.6-max-preview' | 'alibaba/qwen3-235b-a22b-thinking' | 'alibaba/qwen3-coder' | 'alibaba/qwen3-coder-30b-a3b' | 'alibaba/qwen3-coder-next' | 'alibaba/qwen3-coder-plus' | 'alibaba/qwen3-max' | 'alibaba/qwen3-max-preview' | 'alibaba/qwen3-max-thinking' | 'alibaba/qwen3-next-80b-a3b-instruct' | 'alibaba/qwen3-next-80b-a3b-thinking' | 'alibaba/qwen3-vl-235b-a22b-instruct' | 'alibaba/qwen3-vl-instruct' | 'alibaba/qwen3-vl-thinking' | 'alibaba/qwen3.5-flash' | 'alibaba/qwen3.5-plus' | 'alibaba/qwen3.6-27b' | 'alibaba/qwen3.6-plus' | 'alibaba/qwen3.7-flash' | 'alibaba/qwen3.7-max' | 'alibaba/qwen3.7-plus' | 'alibaba/qwen3.8-2.4t-a95b' | 'alibaba/qwen3.8-27b' | 'alibaba/qwen3.8-flash' | 'alibaba/qwen3.8-max' | 'alibaba/qwen3.8-max-0902' | 'amazon/nova-2-lite' | 'amazon/nova-lite' | 'amazon/nova-micro' | 'amazon/nova-pro' | 'anthropic/claude-3-haiku' | 'anthropic/claude-fable-5' | 'anthropic/claude-fable-5.1' | 'anthropic/claude-haiku-4.5' | 'anthropic/claude-opus-4' | 'anthropic/claude-opus-4.5' | 'anthropic/claude-opus-4.6' | 'anthropic/claude-opus-4.7' | 'anthropic/claude-opus-4.8' | 'anthropic/claude-opus-4.8-fast' | 'anthropic/claude-opus-5' | 'anthropic/claude-opus-5-fast' | 'anthropic/claude-sonnet-4' | 'anthropic/claude-sonnet-4.5' | 'anthropic/claude-sonnet-4.6' | 'anthropic/claude-sonnet-5' | 'arcee-ai/trinity-large-thinking' | 'bytedance/seed-1.6' | 'bytedance/seed-1.8' | 'bytedance/seed-2.1-turbo' | 'cohere/command-a' | 'deepseek/deepseek-r1' | 'deepseek/deepseek-v3.1' | 'deepseek/deepseek-v3.1-terminus' | 'deepseek/deepseek-v3.2' | 'deepseek/deepseek-v3.2-thinking' | 'deepseek/deepseek-v4-flash' | 'deepseek/deepseek-v4-flash-0731' | 'deepseek/deepseek-v4-flash-vision-exp' | 'deepseek/deepseek-v4-pro' | 'deepseek/deepseek-v4-pro-0813' | 'deepseek/deepseek-v4.1-flash' | 'google/gemini-2.5-flash' | 'google/gemini-2.5-flash-image' | 'google/gemini-2.5-flash-lite' | 'google/gemini-2.5-pro' | 'google/gemini-3-flash' | 'google/gemini-3-pro-image' | 'google/gemini-3.1-flash-image' | 'google/gemini-3.1-flash-image-preview' | 'google/gemini-3.1-flash-lite' | 'google/gemini-3.1-flash-lite-image' | 'google/gemini-3.1-pro-preview' | 'google/gemini-3.5-flash' | 'google/gemini-3.5-flash-lite' | 'google/gemini-3.6-flash' | 'google/gemini-3.7-flash' | 'google/gemini-3.8-flash' | 'google/gemini-omni-flash-preview' | 'google/gemma-4-26b-a4b-it' | 'google/gemma-4-31b-it' | 'inception/mercury-2' | 'inception/mercury-2.5' | 'inception/mercury-coder-small' | 'inclusionai/ling-3.0-flash' | 'inclusionai/ling-3.0-flash-fin' | 'inclusionai/ling-3.0-flash-fin-free' | 'inclusionai/ling-3.0-flash-sante' | 'inclusionai/ling-3.0-flash-sante-free' | 'inclusionai/ling-3.0-flash-vl' | 'inclusionai/ling-3.0-flash-vl-free' | 'inference-net/schematron-v2-small' | 'inference-net/schematron-v2-turbo' | 'interfaze/interfaze-beta' | 'meta/llama-3.1-70b' | 'meta/llama-3.1-8b' | 'meta/llama-3.3-70b' | 'meta/llama-4-maverick' | 'meta/llama-4-scout' | 'meta/muse-glimmer-30b' | 'meta/muse-spark-1.1' | 'meta/muse-spark-1.2' | 'meta/muse-spark-1.2-contributor' | 'meta/muse-spark-1.3' | 'meta/muse-spark-1.3-contributor' | 'minimax/minimax-m2' | 'minimax/minimax-m2.1' | 'minimax/minimax-m2.1-lightning' | 'minimax/minimax-m2.5' | 'minimax/minimax-m2.5-highspeed' | 'minimax/minimax-m2.7' | 'minimax/minimax-m2.7-highspeed' | 'minimax/minimax-m3' | 'mistral/codestral' | 'mistral/ministral-14b' | 'mistral/ministral-3b' | 'mistral/ministral-8b' | 'mistral/mistral-large-3' | 'mistral/mistral-medium-3.5' | 'mistral/mistral-nemo' | 'mistral/mistral-small' | 'moonshotai/kimi-k2' | 'moonshotai/kimi-k2-thinking' | 'moonshotai/kimi-k2.5' | 'moonshotai/kimi-k2.6' | 'moonshotai/kimi-k2.7-code' | 'moonshotai/kimi-k2.7-code-highspeed' | 'moonshotai/kimi-k3' | 'moonshotai/kimi-k3-fast' | 'morph/morph-v3-fast' | 'morph/morph-v3-large' | 'nvidia/nemotron-3-nano-30b-a3b' | 'nvidia/nemotron-3-super-120b-a12b' | 'nvidia/nemotron-3-ultra-550b-a55b' | 'nvidia/nemotron-3.5-lightning' | 'nvidia/nemotron-nano-12b-v2-vl' | 'nvidia/nemotron-nano-9b-v2' | 'openai/gpt-3.5-turbo' | 'openai/gpt-4-turbo' | 'openai/gpt-4.1' | 'openai/gpt-4.1-fast' | 'openai/gpt-4.1-mini' | 'openai/gpt-4.1-mini-fast' | 'openai/gpt-4.1-nano' | 'openai/gpt-4.1-nano-fast' | 'openai/gpt-4o' | 'openai/gpt-4o-fast' | 'openai/gpt-4o-mini' | 'openai/gpt-4o-mini-fast' | 'openai/gpt-5' | 'openai/gpt-5-codex' | 'openai/gpt-5-fast' | 'openai/gpt-5-mini' | 'openai/gpt-5-mini-fast' | 'openai/gpt-5-nano' | 'openai/gpt-5-pro' | 'openai/gpt-5.1-codex' | 'openai/gpt-5.1-codex-max' | 'openai/gpt-5.1-codex-mini' | 'openai/gpt-5.1-thinking' | 'openai/gpt-5.1-thinking-fast' | 'openai/gpt-5.2' | 'openai/gpt-5.2-codex' | 'openai/gpt-5.2-fast' | 'openai/gpt-5.2-pro' | 'openai/gpt-5.3-codex' | 'openai/gpt-5.3-codex-fast' | 'openai/gpt-5.4' | 'openai/gpt-5.4-fast' | 'openai/gpt-5.4-mini' | 'openai/gpt-5.4-mini-fast' | 'openai/gpt-5.4-nano' | 'openai/gpt-5.4-pro' | 'openai/gpt-5.5' | 'openai/gpt-5.5-fast' | 'openai/gpt-5.5-pro' | 'openai/gpt-5.6-luna' | 'openai/gpt-5.6-luna-fast' | 'openai/gpt-5.6-sol' | 'openai/gpt-5.6-sol-fast' | 'openai/gpt-5.6-terra' | 'openai/gpt-5.6-terra-fast' | 'openai/gpt-6-astra' | 'openai/gpt-6-astra-fast' | 'openai/gpt-oss-120b' | 'openai/gpt-oss-20b' | 'openai/gpt-oss-safeguard-120b' | 'openai/gpt-oss-safeguard-20b' | 'openai/o1' | 'openai/o3' | 'openai/o3-fast' | 'openai/o3-mini' | 'openai/o3-pro' | 'openai/o4-mini' | 'openai/o4-mini-fast' | 'perplexity/sonar' | 'perplexity/sonar-pro' | 'perplexity/sonar-reasoning-pro' | 'poolside/laguna-s-2.1' | 'poolside/laguna-s-2.1-free' | 'sakana/fugu-max' | 'sakana/fugu-ultra' | 'sakana/fugu-ultra-v2' | 'sakana/namazu' | 'spacexai/grok-4.1-fast-non-reasoning' | 'spacexai/grok-4.1-fast-reasoning' | 'spacexai/grok-4.20-multi-agent' | 'spacexai/grok-4.20-multi-agent-beta' | 'spacexai/grok-4.20-non-reasoning' | 'spacexai/grok-4.20-non-reasoning-beta' | 'spacexai/grok-4.20-reasoning' | 'spacexai/grok-4.20-reasoning-beta' | 'spacexai/grok-4.3' | 'spacexai/grok-4.5' | 'spacexai/grok-4.6' | 'spacexai/grok-build-0.1' | 'stepfun/step-3.5-flash' | 'stepfun/step-3.7-flash' | 'tencent/hy-mt2-lite' | 'tencent/hy-mt2-plus' | 'tencent/hy-mt2-pro' | 'tencent/hy3' | 'tencent/hy4-preview' | 'thinkingmachines/inkling' | 'thinkingmachines/inkling-small' | 'xiaomi/mimo-v2.5' | 'xiaomi/mimo-v2.5-pro' | 'zai/glm-4.5' | 'zai/glm-4.5-air' | 'zai/glm-4.5v' | 'zai/glm-4.6' | 'zai/glm-4.7' | 'zai/glm-4.7-flash' | 'zai/glm-4.7-flashx' | 'zai/glm-5' | 'zai/glm-5-turbo' | 'zai/glm-5.1' | 'zai/glm-5.2' | 'zai/glm-5.2-fast' | 'zai/glm-5.3' | 'zai/glm-5.3-fast' | 'zai/glm-5.3-flash' | 'zai/glm-5v-turbo' | (string & {});

/**
 * Shared WebSocket subprotocol contract for AI Gateway realtime and streaming
 * transcription auth.
 *
 * The browser `WebSocket` API cannot set request headers, so the Gateway auth
 * (bearer) token is carried through the `Sec-WebSocket-Protocol` handshake
 * instead of an `Authorization` header — the same workaround OpenAI uses for
 * `openai-insecure-api-key.<token>`.
 *
 * This module is the single source of truth for that contract so the client and
 * the Gateway server can't drift: the client encodes values with
 * `getGatewayRealtimeProtocols` / `getGatewayTranscriptionProtocols`, and the
 * Gateway server decodes them with `getGatewayRealtimeAuthToken` /
 * `getGatewayRealtimeTeamIdOrSlug`.
 *
 * WebSocket subprotocol values must fit the RFC token grammar. The auth token is
 * sent as-is, so callers must use tokens that are valid subprotocol tokens; the
 * optional team scope is base64url-encoded by this module. Keep the complete
 * `Sec-WebSocket-Protocol` header compact (target under an 8 KiB safe header
 * budget) because intermediaries may reject large upgrade headers.
 */
/**
 * Marker subprotocol offered on every realtime handshake so the Gateway can
 * echo a negotiated subprotocol on the 101 response (some clients require the
 * server to select one of the offered subprotocols).
 */
declare const GATEWAY_REALTIME_SUBPROTOCOL = "ai-gateway-realtime.v1";
/**
 * Marker subprotocol for streaming transcription handshakes (same negotiation
 * purpose as `GATEWAY_REALTIME_SUBPROTOCOL`).
 */
declare const GATEWAY_TRANSCRIPTION_SUBPROTOCOL = "ai-gateway-transcription.v1";
/** Subprotocol prefix that carries the Gateway auth (bearer) token. */
declare const GATEWAY_AUTH_SUBPROTOCOL_PREFIX = "ai-gateway-auth.";
/** Subprotocol prefix that carries optional Vercel team scoping. */
declare const GATEWAY_TEAM_SUBPROTOCOL_PREFIX = "ai-gateway-team.";
/**
 * Client-side: build the WebSocket subprotocols that carry `token` to the
 * Gateway. Pass the result as the second argument to `new WebSocket(url, ...)`.
 */
declare function getGatewayRealtimeProtocols(token: string, options?: {
    teamIdOrSlug?: string;
}): string[];
/**
 * Client-side: `getGatewayRealtimeProtocols`, but with the streaming
 * transcription marker subprotocol.
 */
declare function getGatewayTranscriptionProtocols(token: string, options?: {
    teamIdOrSlug?: string;
}): string[];
/**
 * Server-side: extract the Gateway auth (bearer) token from a
 * `Sec-WebSocket-Protocol` header value, or `undefined` when it is absent or
 * empty. The Gateway upgrade handler turns this into an
 * `Authorization: Bearer <token>` before its normal auth path.
 *
 * Accepts the raw header value (subprotocols are comma-separated and may carry
 * surrounding whitespace).
 */
declare function getGatewayRealtimeAuthToken(secWebSocketProtocol: string | null | undefined): string | undefined;
/**
 * Server-side: extract the optional Vercel team ID or slug from the
 * `Sec-WebSocket-Protocol` header value. Team scoping is base64url-encoded so
 * arbitrary team slugs stay within the WebSocket subprotocol token grammar.
 */
declare function getGatewayRealtimeTeamIdOrSlug(secWebSocketProtocol: string | null | undefined): string | undefined;

type GatewayRealtimeModelId = 'google/gemini-3.8-live' | 'google/gemini-3.8-live-extended-thinking' | 'openai/gpt-live-1' | 'openai/gpt-realtime-1.5' | 'openai/gpt-realtime-2' | 'openai/gpt-realtime-2.1' | 'openai/gpt-realtime-mini' | 'spacexai/grok-voice-think-fast-1.0' | 'spacexai/grok-voice-think-fast-2.0' | (string & {});

type GatewayRerankingModelId = 'cohere/rerank-v3.5' | 'cohere/rerank-v4-fast' | 'cohere/rerank-v4-pro' | 'voyage/rerank-2.5' | 'voyage/rerank-2.5-lite' | (string & {});

type GatewaySpeechModelId = 'fish-audio/s1' | 'fish-audio/s1-free' | 'fish-audio/s2-pro' | 'fish-audio/s2-pro-free' | 'fish-audio/s2.1-pro' | 'fish-audio/s2.1-pro-free' | 'openai/tts-1' | 'openai/tts-1-hd' | 'spacexai/grok-tts' | (string & {});

type GatewayTranscriptionModelId = 'fish-audio/transcribe-1' | 'fish-audio/transcribe-1-free' | 'google/gemini-3.5-transcribe' | 'google/gemini-3.5-transcribe-live' | 'openai/gpt-4o-mini-transcribe' | 'openai/gpt-4o-transcribe' | 'openai/gpt-realtime-whisper' | 'openai/whisper-1' | 'spacexai/grok-stt' | (string & {});

type GatewayVideoModelId = 'alibaba/wan-v2.5-t2v-preview' | 'alibaba/wan-v2.6-i2v' | 'alibaba/wan-v2.6-i2v-flash' | 'alibaba/wan-v2.6-r2v' | 'alibaba/wan-v2.6-r2v-flash' | 'alibaba/wan-v2.6-t2v' | 'alibaba/wan-v2.7-r2v' | 'alibaba/wan-v2.7-t2v' | 'alibaba/wan-v3.0-video' | 'alibaba/wan-v3.0-video-prime' | 'bfl/flux-3-video' | 'bytedance/seedance-2.0' | 'bytedance/seedance-2.0-fast' | 'bytedance/seedance-2.0-mini' | 'bytedance/seedance-2.5' | 'bytedance/seedance-v1.0-pro' | 'bytedance/seedance-v1.0-pro-fast' | 'bytedance/seedance-v1.5-pro' | 'google/veo-3.0-fast-generate-001' | 'google/veo-3.0-generate-001' | 'google/veo-3.1-fast-generate-001' | 'google/veo-3.1-generate-001' | 'google/veo-3.1-lite-generate-001' | 'klingai/kling-v2.5-turbo-i2v' | 'klingai/kling-v2.5-turbo-t2v' | 'klingai/kling-v2.6-i2v' | 'klingai/kling-v2.6-motion-control' | 'klingai/kling-v2.6-t2v' | 'klingai/kling-v3.0-i2v' | 'klingai/kling-v3.0-motion-control' | 'klingai/kling-v3.0-t2v' | 'minimax/minimax-h3' | 'minimax/minimax-h3-max' | 'spacexai/grok-imagine-video' | 'spacexai/grok-imagine-video-1.5' | (string & {});

declare const KNOWN_MODEL_TYPES: readonly ["embedding", "evaluation", "image", "language", "realtime", "reranking", "speech", "transcription", "video"];
type KnownModelType = (typeof KNOWN_MODEL_TYPES)[number];
interface GatewayLanguageModelEntry {
    /**
     * The model id used by the remote provider in model settings and for specifying the
     * intended model for text generation.
     */
    id: string;
    /**
     * The display name of the model for presentation in user-facing contexts.
     */
    name: string;
    /**
     * Optional description of the model.
     */
    description?: string | null;
    /**
     * Optional pricing information for the model.
     */
    pricing?: {
        /**
         * Cost per input token in USD.
         */
        input: string;
        /**
         * Cost per output token in USD.
         */
        output: string;
        /**
         * Cost per cached input token in USD.
         * Only present for providers/models that support prompt caching.
         */
        cachedInputTokens?: string;
        /**
         * Cost per input token to create/write cache entries in USD.
         * Only present for providers/models that support prompt caching.
         */
        cacheCreationInputTokens?: string;
    } | null;
    /**
     * Additional AI SDK language model specifications for the model.
     */
    specification: GatewayLanguageModelSpecification;
    /**
     * Optional field to differentiate between model types.
     */
    modelType?: KnownModelType | null;
}
type GatewayLanguageModelSpecification = Pick<LanguageModelV4, 'specificationVersion' | 'provider' | 'modelId'>;

interface GatewayFetchMetadataResponse {
    models: GatewayLanguageModelEntry[];
}
interface GatewayCreditsResponse {
    /** The remaining gateway credit balance available for API usage */
    balance: string;
    /** The total amount of gateway credits that have been consumed */
    totalUsed: string;
}

interface GatewaySpendReportParams {
    /** Start date in YYYY-MM-DD format (inclusive) */
    startDate: string;
    /** End date in YYYY-MM-DD format (inclusive) */
    endDate: string;
    /** Primary aggregation dimension. Defaults to 'day'. */
    groupBy?: 'day' | 'user' | 'model' | 'tag' | 'provider' | 'credential_type';
    /** Time granularity when groupBy is 'day'. */
    datePart?: 'day' | 'hour';
    /** Filter to a specific user's spend. */
    userId?: string;
    /** Filter to a specific model (e.g. 'anthropic/claude-sonnet-4.5'). */
    model?: string;
    /** Filter to a specific provider (e.g. 'anthropic'). */
    provider?: string;
    /** Filter to BYOK or system credentials. */
    credentialType?: 'byok' | 'system';
    /** Filter to requests with these tags. */
    tags?: string[];
}
interface GatewaySpendReportRow {
    /** Date string (present when groupBy is 'day') */
    day?: string;
    /** Hour timestamp (present when groupBy is 'day' and datePart is 'hour') */
    hour?: string;
    /** User identifier (present when groupBy is 'user') */
    user?: string;
    /** Model identifier (present when groupBy is 'model') */
    model?: string;
    /** Tag value (present when groupBy is 'tag') */
    tag?: string;
    /** Provider name (present when groupBy is 'provider') */
    provider?: string;
    /** Credential type (present when groupBy is 'credential_type') */
    credentialType?: 'byok' | 'system';
    /** Total cost in USD */
    totalCost: number;
    /** Market cost in USD */
    marketCost?: number;
    /** Number of input tokens */
    inputTokens?: number;
    /** Number of output tokens */
    outputTokens?: number;
    /** Number of cached input tokens */
    cachedInputTokens?: number;
    /** Number of cache creation input tokens */
    cacheCreationInputTokens?: number;
    /** Number of reasoning tokens */
    reasoningTokens?: number;
    /** Number of requests */
    requestCount?: number;
}
interface GatewaySpendReportResponse {
    results: GatewaySpendReportRow[];
}

interface GatewayGenerationInfoParams {
    /** The generation ID to look up (format: gen_<ulid>) */
    id: string;
}
interface GatewayGenerationInfo {
    /** The generation ID */
    id: string;
    /** Total cost in USD */
    totalCost: number;
    /** Upstream inference cost in USD (BYOK only) */
    upstreamInferenceCost: number;
    /** Usage cost in USD (same as totalCost) */
    usage: number;
    /** ISO 8601 timestamp when the generation was created */
    createdAt: string;
    /** Model identifier */
    model: string;
    /** Whether BYOK credentials were used */
    isByok: boolean;
    /** Provider that served this generation */
    providerName: string;
    /** Whether streaming was used */
    streamed: boolean;
    /** Finish reason (e.g. 'stop') */
    finishReason: string;
    /** Time to first token in milliseconds */
    latency: number;
    /** Total generation time in milliseconds */
    generationTime: number;
    /** Number of prompt tokens */
    promptTokens: number;
    /** Number of completion tokens */
    completionTokens: number;
    /** Reasoning tokens used */
    reasoningTokens: number;
    /** Cached tokens used */
    cachedTokens: number;
    /** Cache creation input tokens */
    cacheCreationTokens: number;
    /** Billable web search calls */
    billableWebSearchCalls: number;
}

type TakoSearchEffort = 'deep' | 'fast' | 'instant';
type TakoContentFormat = 'card_json' | 'csv' | 'json_compact' | 'json_records';
interface TakoDataSourceConfig {
    /**
     * Maximum number of data results to return (1-20). When includeContents is
     * true, each additional result adds its own data surcharge.
     */
    count?: number;
    /**
     * Inline rows for each data result. This adds a data surcharge based on row
     * count and dataset source. To estimate cost, search with includeContents
     * disabled and inspect cards.content.export_pricing. This applies to every
     * returned card; limit sources.data.count and sources.data.maxRows to control
     * cost.
     */
    includeContents?: boolean;
    /** Requested delivery mode for card data. Search cards are always inlined. */
    mode?: 'inline' | 'url';
    /** Serialization for inlined card data. */
    contentFormat?: TakoContentFormat;
    /**
     * Maximum rows to inline per result. Omit to use the allowance in
     * cards.content.export_pricing. A data surcharge applies per 1,000 exported
     * rows; lower values reduce cost.
     */
    maxRows?: number;
    /** Data Graph node IDs to prioritize. */
    nodeIds?: string[];
    /** Return only cards matching nodeIds. Requires at least one node ID. */
    strict?: boolean;
}
interface TakoWebSourceConfig {
    /** Maximum number of web results to return (1-20). */
    count?: number;
    /** Inline extracted web page text. This can add a data charge. */
    includeContents?: boolean;
    /** Optional web-result category filter. */
    category?: 'finance' | 'news' | 'sports';
    /** Only return results from these bare domains. */
    includeDomains?: string[];
    /** Exclude results from these bare domains. */
    excludeDomains?: string[];
    /** Maximum characters in each web-result snippet. */
    snippetMaxChars?: number;
    /** Include highlighted passages in web results. Defaults to true in AI Gateway. */
    highlights?: boolean;
    /** Maximum extracted characters per web page when including contents. */
    articleContentMaxChars?: number;
    /** Keep results published on or after this ISO date. */
    publishedAfter?: string;
    /** Keep results published on or before this ISO date. */
    publishedBefore?: string;
}
interface TakoSearchConfig {
    /** Search effort. fast is the default balanced option. */
    effort?: TakoSearchEffort;
    /** Sources to search. Omit to search both curated data and the web. */
    sources?: {
        data?: TakoDataSourceConfig;
        web?: TakoWebSourceConfig;
    };
    /** End-user coordinates for localized results. */
    location?: {
        latitude: number;
        longitude: number;
    };
    /** ISO 3166-1 alpha-2 country code. */
    countryCode?: string;
    /** BCP-47 locale. */
    locale?: string;
    /** IANA timezone. */
    timezone?: string;
    /** Settings that control card rendering. */
    outputSettings?: {
        imageDarkMode?: boolean;
        /** Instant-effort only. */
        forceRefresh?: boolean;
    };
    /** Maximum related search suggestions to include (1-20). */
    includeRelated?: number;
}
interface TakoSearchInput {
    query: string;
    effort?: TakoSearchEffort;
    sources?: {
        data?: {
            count?: number;
            include_contents?: boolean;
            mode?: 'inline' | 'url';
            content_format?: TakoContentFormat;
            max_rows?: number;
            node_ids?: string[];
            strict?: boolean;
        };
        web?: {
            count?: number;
            include_contents?: boolean;
            category?: 'finance' | 'news' | 'sports';
            include_domains?: string[];
            exclude_domains?: string[];
            snippet_max_chars?: number;
            highlights?: boolean;
            article_content_max_chars?: number;
            published_after?: string;
            published_before?: string;
        };
    };
    location?: {
        latitude: number;
        longitude: number;
    };
    country_code?: string;
    locale?: string;
    timezone?: string;
    output_settings?: {
        image_dark_mode?: boolean;
        force_refresh?: boolean;
    };
    include_related?: number;
}
type TakoDatasetCell = boolean | number | string | null;
interface TakoResultContent {
    content_format?: TakoContentFormat | null;
    cost?: number;
    data?: string | null;
    records?: Array<Record<string, TakoDatasetCell>> | null;
    dataset?: {
        columns: Array<{
            name: string;
            type: 'boolean' | 'date' | 'datetime' | 'number' | 'string';
            unit?: string | null;
        }>;
        rows: TakoDatasetCell[][];
        total_rows: number;
        truncated: boolean;
        ref: string;
        sources: Array<{
            name: string;
            index?: 'data' | 'web';
        }>;
        provenance?: 'query' | 'web_extraction';
    } | null;
    card_data?: Record<string, unknown> | null;
    card_data_schema?: Record<string, unknown> | null;
    url?: string | null;
    expires_at?: string | null;
    total_rows?: number | null;
    truncated?: boolean;
    export_pricing?: {
        baseline_usd: number;
        free_rows: number;
        max_rows_ceiling: number;
        row_cpm_usd: number;
    } | null;
    manifest?: Array<{
        dtype?: 'boolean' | 'date' | 'datetime' | 'number' | 'string' | null;
        entity?: string | null;
        metric?: string | null;
        name?: string | null;
        unit?: string | null;
    }> | null;
}
interface TakoCard {
    card_id?: string | null;
    title?: string | null;
    description?: string | null;
    semantic_description?: string | null;
    webpage_url?: string | null;
    image_url?: string | null;
    embed_url?: string | null;
    sources?: Array<{
        source_name?: string | null;
        source_description?: string | null;
        source_index: 'data' | 'web';
        source_text?: string | null;
        url?: string | null;
    }> | null;
    methodologies?: Array<{
        methodology_name: string | null;
        methodology_description: string | null;
    }> | null;
    source_indexes?: Array<'data' | 'web'> | null;
    card_type?: string | null;
    relevance?: 'High' | 'Low' | 'Medium' | null;
    content?: TakoResultContent | null;
    exportable?: boolean;
    nodes?: Array<{
        id: string;
        type: 'entity' | 'metric';
        name: string;
        description?: string | null;
    }> | null;
    metric_definitions?: Array<{
        name: string;
        definition: string;
    }> | null;
    data_freshness?: {
        coverage_end?: string | null;
        data_as_of?: string | null;
        last_updated?: string | null;
    } | null;
}
interface TakoWebResult {
    title: string;
    url: string;
    snippet?: string | null;
    source_name?: string | null;
    publish_date?: string | null;
    content?: TakoResultContent | null;
}
interface TakoSearchResponse {
    request_id: string;
    cards?: TakoCard[];
    web_results?: TakoWebResult[];
    usage?: {
        total_cost_usd: number;
        compute?: {
            cost_usd: number;
        } | null;
        data?: {
            cost_usd: number;
            datasets: number;
        } | null;
    } | null;
    related?: Array<Record<string, unknown>> | null;
}
interface TakoSearchError {
    error: 'api_error' | 'configuration_error' | 'execution_error' | 'invalid_input' | 'rate_limit' | 'timeout' | 'unknown_tool';
    statusCode?: number;
    message: string;
}
type TakoSearchOutput = TakoSearchError | TakoSearchResponse;
declare const takoSearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<TakoSearchInput, TakoSearchOutput, TakoSearchConfig, {}>;

interface PerplexitySearchConfig {
    /**
     * Default maximum number of search results to return (1-20, default: 10).
     */
    maxResults?: number;
    /**
     * Default maximum tokens to extract per search result page (256-2048, default: 2048).
     */
    maxTokensPerPage?: number;
    /**
     * Default maximum total tokens across all search results (default: 25000, max: 1000000).
     */
    maxTokens?: number;
    /**
     * Default two-letter ISO 3166-1 alpha-2 country code for regional search results.
     * Examples: 'US', 'GB', 'FR'
     */
    country?: string;
    /**
     * Default list of domains to include or exclude from search results (max 20).
     * To include: ['nature.com', 'science.org']
     * To exclude: ['-example.com', '-spam.net']
     */
    searchDomainFilter?: string[];
    /**
     * Default list of ISO 639-1 language codes to filter results (max 10, lowercase).
     * Examples: ['en', 'fr', 'de']
     */
    searchLanguageFilter?: string[];
    /**
     * Default recency filter for results.
     * Cannot be combined with searchAfterDate/searchBeforeDate at runtime.
     */
    searchRecencyFilter?: 'day' | 'week' | 'month' | 'year';
}
interface PerplexitySearchResult {
    /** Title of the search result */
    title: string;
    /** URL of the search result */
    url: string;
    /** Text snippet/preview of the content */
    snippet: string;
    /** Publication date of the content */
    date?: string;
    /** Last updated date of the content */
    lastUpdated?: string;
}
interface PerplexitySearchResponse {
    /** Array of search results */
    results: PerplexitySearchResult[];
    /** Unique identifier for this search request */
    id: string;
}
interface PerplexitySearchError {
    /** Error type */
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'unknown';
    /** HTTP status code if applicable */
    statusCode?: number;
    /** Human-readable error message */
    message: string;
}
interface PerplexitySearchInput {
    /**
     * Search query (string) or multiple queries (array of up to 5 strings).
     * Multi-query searches return combined results from all queries.
     */
    query: string | string[];
    /**
     * Maximum number of search results to return (1-20, default: 10).
     */
    max_results?: number;
    /**
     * Maximum number of tokens to extract per search result page (256-2048, default: 2048).
     */
    max_tokens_per_page?: number;
    /**
     * Maximum total tokens across all search results (default: 25000, max: 1000000).
     */
    max_tokens?: number;
    /**
     * Two-letter ISO 3166-1 alpha-2 country code for regional search results.
     * Examples: 'US', 'GB', 'FR'
     */
    country?: string;
    /**
     * List of domains to include or exclude from search results (max 20).
     * To include: ['nature.com', 'science.org']
     * To exclude: ['-example.com', '-spam.net']
     */
    search_domain_filter?: string[];
    /**
     * List of ISO 639-1 language codes to filter results (max 10, lowercase).
     * Examples: ['en', 'fr', 'de']
     */
    search_language_filter?: string[];
    /**
     * Include only results published after this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/1/2025')
     * Cannot be used with search_recency_filter.
     */
    search_after_date?: string;
    /**
     * Include only results published before this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/15/2025')
     * Cannot be used with search_recency_filter.
     */
    search_before_date?: string;
    /**
     * Include only results last updated after this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/1/2025')
     * Cannot be used with search_recency_filter.
     */
    last_updated_after_filter?: string;
    /**
     * Include only results last updated before this date.
     * Format: 'MM/DD/YYYY' (e.g., '3/15/2025')
     * Cannot be used with search_recency_filter.
     */
    last_updated_before_filter?: string;
    /**
     * Filter results by relative time period.
     * Cannot be used with search_after_date or search_before_date.
     */
    search_recency_filter?: 'day' | 'week' | 'month' | 'year';
}
type PerplexitySearchOutput = PerplexitySearchResponse | PerplexitySearchError;
declare const perplexitySearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<PerplexitySearchInput, PerplexitySearchOutput, PerplexitySearchConfig, {}>;

interface ParallelSearchSourcePolicy {
    /**
     * List of domains to include in search results.
     * Example: ['wikipedia.org', 'nature.com']
     */
    includeDomains?: string[];
    /**
     * List of domains to exclude from search results.
     * Example: ['reddit.com', 'twitter.com']
     */
    excludeDomains?: string[];
    /**
     * Only include results published after this date (ISO 8601 format).
     * Example: '2024-01-01'
     */
    afterDate?: string;
}
interface ParallelSearchExcerpts {
    /**
     * Maximum characters per result.
     */
    maxCharsPerResult?: number;
    /**
     * Maximum total characters across all results.
     */
    maxCharsTotal?: number;
}
interface ParallelSearchFetchPolicy {
    /**
     * Maximum age in seconds for cached content.
     * Set to 0 to always fetch fresh content.
     */
    maxAgeSeconds?: number;
}
interface ParallelSearchConfig {
    /**
     * Mode preset for different use cases:
     * - "one-shot": Comprehensive results with longer excerpts for single-response answers (default)
     * - "agentic": Concise, token-efficient results for multi-step agentic workflows
     */
    mode?: 'one-shot' | 'agentic';
    /**
     * Default maximum number of results to return (1-20).
     * Defaults to 10 if not specified.
     */
    maxResults?: number;
    /**
     * Default source policy for controlling which domains to include/exclude.
     */
    sourcePolicy?: ParallelSearchSourcePolicy;
    /**
     * Default excerpt configuration for controlling result length.
     */
    excerpts?: ParallelSearchExcerpts;
    /**
     * Default fetch policy for controlling content freshness.
     */
    fetchPolicy?: ParallelSearchFetchPolicy;
}
interface ParallelSearchResult {
    /** URL of the search result */
    url: string;
    /** Title of the search result */
    title: string;
    /** Extracted text excerpt/content from the page */
    excerpt: string;
    /** Publication date of the content (may be null) */
    publishDate?: string | null;
    /** Relevance score for the result */
    relevanceScore?: number;
}
interface ParallelSearchResponse {
    /** Unique identifier for this search request */
    searchId: string;
    /** Array of search results */
    results: ParallelSearchResult[];
}
interface ParallelSearchError {
    /** Error type */
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'configuration_error' | 'unknown';
    /** HTTP status code if applicable */
    statusCode?: number;
    /** Human-readable error message */
    message: string;
}
interface ParallelSearchInput {
    /**
     * Natural-language description of the web research goal.
     * Include source or freshness guidance and broader context from the task.
     * Maximum 5000 characters.
     */
    objective: string;
    /**
     * Optional search queries to supplement the objective.
     * Maximum 200 characters per query.
     */
    search_queries?: string[];
    /**
     * Mode preset for different use cases:
     * - "one-shot": Comprehensive results with longer excerpts
     * - "agentic": Concise, token-efficient results for multi-step workflows
     */
    mode?: 'one-shot' | 'agentic';
    /**
     * Maximum number of results to return (1-20).
     * Defaults to 10 if not specified.
     */
    max_results?: number;
    /**
     * Source policy for controlling which domains to include/exclude.
     */
    source_policy?: {
        include_domains?: string[];
        exclude_domains?: string[];
        after_date?: string;
    };
    /**
     * Excerpt configuration for controlling result length.
     */
    excerpts?: {
        max_chars_per_result?: number;
        max_chars_total?: number;
    };
    /**
     * Fetch policy for controlling content freshness.
     */
    fetch_policy?: {
        max_age_seconds?: number;
    };
}
type ParallelSearchOutput = ParallelSearchResponse | ParallelSearchError;
declare const parallelSearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<ParallelSearchInput, ParallelSearchOutput, ParallelSearchConfig, {}>;

type ExaSearchType = 'auto' | 'fast' | 'instant';
type ExaSearchCategory = 'company' | 'people' | 'research paper' | 'news' | 'personal site' | 'financial report';
type ExaTextSection = 'header' | 'navigation' | 'banner' | 'body' | 'sidebar' | 'footer' | 'metadata';
interface ExaSearchTextConfig {
    maxCharacters?: number;
    includeHtmlTags?: boolean;
    verbosity?: 'compact' | 'standard' | 'full';
    includeSections?: ExaTextSection[];
    excludeSections?: ExaTextSection[];
}
interface ExaSearchHighlightsConfig {
    query?: string;
    maxCharacters?: number;
}
interface ExaSearchExtrasConfig {
    links?: number;
    imageLinks?: number;
}
interface ExaSearchContentsConfig {
    text?: boolean | ExaSearchTextConfig;
    highlights?: boolean | ExaSearchHighlightsConfig;
    maxAgeHours?: number;
    livecrawlTimeout?: number;
    subpages?: number;
    subpageTarget?: string | string[];
    extras?: ExaSearchExtrasConfig;
}
interface ExaSearchConfig {
    /**
     * Default search method. Exa defaults to auto when omitted.
     */
    type?: ExaSearchType;
    /**
     * Default maximum number of results to return (1-100, default: 10).
     */
    numResults?: number;
    /**
     * Default category filter for result types.
     */
    category?: ExaSearchCategory;
    /**
     * Default two-letter ISO country code for location-aware search.
     */
    userLocation?: string;
    /**
     * Default domains to include or exclude.
     */
    includeDomains?: string[];
    excludeDomains?: string[];
    /**
     * Default published date filters in ISO 8601 format.
     */
    startPublishedDate?: string;
    endPublishedDate?: string;
    /**
     * Default content extraction controls.
     */
    contents?: ExaSearchContentsConfig;
}
interface ExaSearchResult {
    title: string;
    url: string;
    id: string;
    publishedDate?: string | null;
    author?: string | null;
    image?: string | null;
    favicon?: string | null;
    text?: string;
    highlights?: string[];
    highlightScores?: number[];
    summary?: string;
    subpages?: ExaSearchResult[];
    extras?: {
        links?: string[];
        imageLinks?: string[];
    };
}
interface ExaSearchResponse {
    requestId: string;
    searchType?: string;
    resolvedSearchType?: string;
    results: ExaSearchResult[];
    costDollars?: {
        total?: number;
        search?: Record<string, number>;
    };
}
interface ExaSearchError {
    error: 'api_error' | 'rate_limit' | 'timeout' | 'invalid_input' | 'configuration_error' | 'execution_error' | 'unknown';
    statusCode?: number;
    message: string;
}
interface ExaSearchInput {
    query: string;
    type?: ExaSearchType;
    num_results?: number;
    category?: ExaSearchCategory;
    user_location?: string;
    include_domains?: string[];
    exclude_domains?: string[];
    start_published_date?: string;
    end_published_date?: string;
    contents?: {
        text?: boolean | {
            max_characters?: number;
            include_html_tags?: boolean;
            verbosity?: 'compact' | 'standard' | 'full';
            include_sections?: ExaTextSection[];
            exclude_sections?: ExaTextSection[];
        };
        highlights?: boolean | {
            query?: string;
            max_characters?: number;
        };
        max_age_hours?: number;
        livecrawl_timeout?: number;
        subpages?: number;
        subpage_target?: string | string[];
        extras?: {
            links?: number;
            image_links?: number;
        };
    };
}
type ExaSearchOutput = ExaSearchResponse | ExaSearchError;
declare const exaSearchToolFactory: _ai_sdk_provider_utils.ProviderExecutedToolFactory<ExaSearchInput, ExaSearchOutput, ExaSearchConfig, {}>;

/**
 * Gateway-specific provider-defined tools.
 */
declare const gatewayTools: {
    /**
     * Search the web using Exa for current information and token-efficient
     * excerpts optimized for agent workflows.
     *
     * Supports search type, category, domain, date, location, and content
     * extraction controls.
     */
    exaSearch: (config?: ExaSearchConfig) => ReturnType<typeof exaSearchToolFactory>;
    /**
     * Search the web using Parallel AI's Search API for LLM-optimized excerpts.
     *
     * Takes a natural language objective and returns relevant excerpts,
     * replacing multiple keyword searches with a single call for broad
     * or complex queries. Supports different search types for depth vs
     * breadth tradeoffs.
     */
    parallelSearch: (config?: ParallelSearchConfig) => ReturnType<typeof parallelSearchToolFactory>;
    /**
     * Search the web using Perplexity's Search API for real-time information,
     * news, research papers, and articles.
     *
     * Provides ranked search results with advanced filtering options including
     * domain, language, date range, and recency filters.
     */
    perplexitySearch: (config?: PerplexitySearchConfig) => ReturnType<typeof perplexitySearchToolFactory>;
    /**
     * Search the web and Tako's curated knowledge graph in one call for
     * token-efficient web excerpts and structured data results grounded in
     * premium sources, each with an embed-ready visualization.
     *
     * Supports effort, per-source web and data controls, localization, and inline
     * contents for agents that need to reason over underlying data.
     */
    takoSearch: (config?: TakoSearchConfig) => ReturnType<typeof takoSearchToolFactory>;
};

interface GatewayProvider extends ProviderV4 {
    (modelId: GatewayModelId): LanguageModelV4;
    /**
     * Creates a model for text generation.
     */
    chat(modelId: GatewayModelId): LanguageModelV4;
    /**
     * Creates a model for text generation.
     */
    languageModel(modelId: GatewayModelId): LanguageModelV4;
    /** Returns a BatchV4 interface for processing batches with AI Gateway. */
    experimental_batch(): Experimental_BatchV4<{
        text: GatewayModelId;
    }>;
    /**
     * Returns available providers and models for use with the remote provider.
     */
    getAvailableModels(): Promise<GatewayFetchMetadataResponse>;
    /**
     * Returns credit information for the authenticated user.
     */
    getCredits(): Promise<GatewayCreditsResponse>;
    /**
     * Returns a spend report with cost, token, and request count data,
     * aggregated by the specified dimension.
     */
    getSpendReport(params: GatewaySpendReportParams): Promise<GatewaySpendReportResponse>;
    /**
     * Returns detailed information about a specific generation by its ID,
     * including cost, token usage, latency, and provider details.
     */
    getGenerationInfo(params: GatewayGenerationInfoParams): Promise<GatewayGenerationInfo>;
    /**
     * Creates a model for generating text embeddings.
     */
    embedding(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * Creates a model for generating text embeddings.
     */
    embeddingModel(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * @deprecated Use `embeddingModel` instead.
     */
    textEmbeddingModel(modelId: GatewayEmbeddingModelId): EmbeddingModelV4;
    /**
     * Creates a model for generating images.
     */
    image(modelId: GatewayImageModelId): ImageModelV4;
    /**
     * Creates a model for generating images.
     */
    imageModel(modelId: GatewayImageModelId): ImageModelV4;
    /**
     * Creates a model for generating videos.
     */
    video(modelId: GatewayVideoModelId): Experimental_VideoModelV4;
    /**
     * Creates a model for generating videos.
     */
    videoModel(modelId: GatewayVideoModelId): Experimental_VideoModelV4;
    /**
     * Creates a model for reranking documents.
     */
    reranking(modelId: GatewayRerankingModelId): RerankingModelV4;
    /**
     * Creates a model for reranking documents.
     */
    rerankingModel(modelId: GatewayRerankingModelId): RerankingModelV4;
    /**
     * Creates a model for evaluating state against questions.
     */
    evaluation(modelId: GatewayEvaluationModelId): Experimental_EvaluationModelV4;
    /**
     * Creates a model for evaluating state against questions.
     */
    evaluationModel(modelId: GatewayEvaluationModelId): Experimental_EvaluationModelV4;
    /**
     * Creates a model for text-to-speech generation.
     */
    speech(modelId: GatewaySpeechModelId): SpeechModelV4;
    /**
     * Creates a model for text-to-speech generation.
     */
    speechModel(modelId: GatewaySpeechModelId): SpeechModelV4;
    /**
     * Creates a model for audio transcription.
     */
    transcription(modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    /**
     * Creates a model for audio transcription.
     */
    transcriptionModel(modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    /**
     * Creates an experimental realtime model for bidirectional audio/text
     * communication over WebSocket, normalized through the AI Gateway.
     */
    experimental_realtime: Experimental_RealtimeFactoryV4;
    /**
     * Experimental streaming-transcription entry point. Callable like
     * `transcription(modelId)`, plus `getToken` for minting a short-lived
     * client secret (`vcst_`) a browser can use to open the streaming
     * transcription WebSocket without holding the Gateway credential.
     */
    experimental_transcription: GatewayTranscriptionFactory;
    /**
     * Gateway-specific tools executed server-side.
     */
    tools: typeof gatewayTools;
}
type GatewayTranscriptionFactoryGetTokenOptions = {
    model: GatewayTranscriptionModelId;
    /** Token lifetime in seconds. Gateway default is 60s (max 300s). */
    expiresAfterSeconds?: number;
};
type GatewayTranscriptionFactoryGetTokenResult = {
    /** The minted `vcst_` client secret. */
    token: string;
    /** WebSocket URL of the streaming transcription surface for this model. */
    url: string;
    /** Token expiry, epoch seconds. */
    expiresAt?: number;
};
/**
 * Streaming-transcription factory: callable like `transcription(modelId)`,
 * plus a server-side `getToken` that mints a transcription-bound short-lived
 * client secret (`vcst_`). The browser connects with
 * `createGateway({ apiKey: token }).transcription(modelId)` — the token rides
 * the same auth subprotocol an API key does, without exposing the credential.
 */
interface GatewayTranscriptionFactory {
    (modelId: GatewayTranscriptionModelId): TranscriptionModelV4;
    getToken(options: GatewayTranscriptionFactoryGetTokenOptions): Promise<GatewayTranscriptionFactoryGetTokenResult>;
}
interface GatewayProviderSettings {
    /**
     * The base URL prefix for API calls. Defaults to `https://ai-gateway.vercel.sh/v4/ai`.
     */
    baseURL?: string;
    /**
     * API key or Vercel access token that is being sent using the `Authorization`
     * header. It defaults to the `AI_GATEWAY_API_KEY` environment variable.
     */
    apiKey?: string;
    /**
     * Vercel team ID or slug to scope requests for access tokens that can access
     * multiple teams.
     */
    teamIdOrSlug?: string;
    /**
     * Custom headers to include in the requests.
     */
    headers?: Record<string, string>;
    /**
     * Custom fetch implementation. You can use it as a middleware to intercept requests,
     * or to provide a custom fetch implementation for e.g. testing.
     */
    fetch?: FetchFunction;
    /**
     * Custom WebSocket implementation used for streaming transcription. This is
     * useful for testing or for runtimes without a global WebSocket. A
     * header-capable implementation is not required — Gateway WebSocket auth is
     * carried in the subprotocols.
     */
    webSocket?: WebSocketConstructor;
    /**
     * How frequently to refresh the metadata cache in milliseconds.
     */
    metadataCacheRefreshMillis?: number;
}
/**
 * Create a remote provider instance.
 */
declare function createGateway(options?: GatewayProviderSettings): GatewayProvider;
declare const gateway: GatewayProvider;

/**
 * Metadata for an asynchronous AI Gateway job.
 */
type GatewayAsyncJobMetadata = {
    readonly jobId: string;
    readonly status: string;
    /**
     * Secret for verifying customer webhook deliveries. This is sensitive,
     * start-response-only metadata and must not be logged or forwarded.
     */
    readonly webhookSigningSecret?: string;
    readonly [key: string]: JSONValue | undefined;
};
/**
 * Shape of the inner `providerMetadata.gateway` object returned by AI Gateway
 * operations.
 *
 * Additional fields may be added without a package update.
 */
type GatewayProviderMetadata = {
    readonly asyncJob?: GatewayAsyncJobMetadata;
    readonly [key: string]: JSONValue | undefined;
};

type GatewayProviderOptions = {
    /**
     * Service-owned options may be added by the Gateway without requiring an SDK
     * release. The Gateway service validates and applies the runtime schema.
     */
    [key: string]: unknown;
    /** Request-scoped BYOK credentials to use instead of cached credentials. */
    byok?: Record<string, Array<Record<string, unknown>>>;
    /** Enables automatic caching behavior when supported by the Gateway. */
    caching?: 'auto';
    /** Filter to providers that do not train on prompt data. */
    disallowPromptTraining?: boolean;
    /**
     * Restrict routing to models that have all of the given capabilities.
     * Currently supports `'implicit-caching'` and `'vision'` (image input).
     */
    has?: Array<'implicit-caching' | 'vision'>;
    /**
     * Idempotency key for `experimental_startBatch`: retries with the same
     * key replay the original batch instead of creating a duplicate.
     */
    idempotencyKey?: string;
    /** Array of model slugs specifying fallback models to use in order. */
    models?: string[];
    /** Array of provider slugs that are the only ones allowed to be used. */
    only?: string[];
    /** Array of provider slugs specifying the provider attempt order. */
    order?: string[];
    /** Per-provider timeouts for BYOK credentials in milliseconds. */
    providerTimeouts?: {
        byok?: Record<string, number>;
    };
    /** Entity identifier against which quota is tracked. */
    quotaEntityId?: string;
    /** Unified service tier intent. */
    serviceTier?: 'flex' | 'priority';
    /** Sort providers by a performance or cost metric before routing. */
    sort?: 'cost' | 'tps' | 'ttft';
    /** User-specified tags for reporting and filtering usage. */
    tags?: string[];
    /** End-user identifier for spend tracking and attribution. */
    user?: string;
    /** Filter to providers with zero data retention agreements. */
    zeroDataRetention?: boolean;
};

declare const symbol$9: unique symbol;
declare abstract class GatewayError extends Error {
    private readonly [symbol$9];
    abstract readonly name: string;
    abstract readonly type: string;
    readonly statusCode: number;
    readonly cause?: unknown;
    readonly generationId?: string;
    readonly isRetryable: boolean;
    constructor({ message, statusCode, cause, generationId, isRetryable, }: {
        message: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
        isRetryable?: boolean;
    });
    /**
     * Checks if the given error is a Gateway Error.
     * @param {unknown} error - The error to check.
     * @returns {boolean} True if the error is a Gateway Error, false otherwise.
     */
    static isInstance(error: unknown): error is GatewayError;
    static hasMarker(error: unknown): error is GatewayError;
}

declare const gatewayErrorResponseSchema: _ai_sdk_provider_utils.LazySchema<{
    error: {
        message: string;
        type?: string | null | undefined;
        param?: unknown;
        code?: string | number | null | undefined;
    };
    generationId?: string | null | undefined;
}>;
type GatewayErrorResponse = InferSchema<typeof gatewayErrorResponseSchema>;

declare const symbol$8: unique symbol;
/**
 * Authentication failed - invalid API key or OIDC token
 */
declare class GatewayAuthenticationError extends GatewayError {
    private readonly [symbol$8];
    readonly name = "GatewayAuthenticationError";
    readonly type = "authentication_error";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayAuthenticationError;
    /**
     * Creates a contextual error message when authentication fails
     */
    static createContextualError({ apiKeyProvided, oidcTokenProvided, statusCode, cause, generationId, }: {
        apiKeyProvided: boolean;
        oidcTokenProvided: boolean;
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    }): GatewayAuthenticationError;
}

declare const symbol$7: unique symbol;
/**
 * The request could not be fulfilled because a dependency it relied on was not
 * available on the credentials or provider used to serve it (HTTP 424). Not
 * retryable — the caller must change the request.
 */
declare class GatewayFailedDependencyError extends GatewayError {
    private readonly [symbol$7];
    readonly name = "GatewayFailedDependencyError";
    readonly type = "failed_dependency";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayFailedDependencyError;
}

declare const symbol$6: unique symbol;
/**
 * Forbidden - the request was rejected by policy (e.g. a routing rule),
 * not an authentication failure.
 */
declare class GatewayForbiddenError extends GatewayError {
    private readonly [symbol$6];
    readonly name = "GatewayForbiddenError";
    readonly type = "forbidden";
    readonly ruleId?: string;
    constructor({ message, statusCode, cause, generationId, ruleId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
        ruleId?: string;
    });
    static isInstance(error: unknown): error is GatewayForbiddenError;
}

declare const symbol$5: unique symbol;
/**
 * Internal server error from the Gateway
 */
declare class GatewayInternalServerError extends GatewayError {
    private readonly [symbol$5];
    readonly name = "GatewayInternalServerError";
    readonly type = "internal_server_error";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayInternalServerError;
}

declare const symbol$4: unique symbol;
/**
 * Invalid request - missing headers, malformed data, etc.
 */
declare class GatewayInvalidRequestError extends GatewayError {
    private readonly [symbol$4];
    readonly name = "GatewayInvalidRequestError";
    readonly type = "invalid_request_error";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayInvalidRequestError;
}

declare const symbol$3: unique symbol;
/**
 * Model not found or not available
 */
declare class GatewayModelNotFoundError extends GatewayError {
    private readonly [symbol$3];
    readonly name = "GatewayModelNotFoundError";
    readonly type = "model_not_found";
    readonly modelId?: string;
    constructor({ message, statusCode, modelId, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        modelId?: string;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayModelNotFoundError;
}

declare const symbol$2: unique symbol;
/**
 * Not found - the requested Gateway resource does not exist or is not
 * visible to the caller (e.g. an unknown async batch/video job id).
 * Distinct from `GatewayModelNotFoundError`, which is model-specific.
 */
declare class GatewayNotFoundError extends GatewayError {
    private readonly [symbol$2];
    readonly name = "GatewayNotFoundError";
    readonly type = "not_found";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayNotFoundError;
}

declare const symbol$1: unique symbol;
/**
 * Rate limit exceeded.
 */
declare class GatewayRateLimitError extends GatewayError {
    private readonly [symbol$1];
    readonly name = "GatewayRateLimitError";
    readonly type = "rate_limit_exceeded";
    constructor({ message, statusCode, cause, generationId, }?: {
        message?: string;
        statusCode?: number;
        cause?: unknown;
        generationId?: string;
    });
    static isInstance(error: unknown): error is GatewayRateLimitError;
}

declare const symbol: unique symbol;
/**
 * Gateway response parsing error
 */
declare class GatewayResponseError extends GatewayError {
    private readonly [symbol];
    readonly name = "GatewayResponseError";
    readonly type = "response_error";
    readonly response?: unknown;
    readonly validationError?: TypeValidationError;
    constructor({ message, statusCode, response, validationError, cause, generationId, isRetryable, }?: {
        message?: string;
        statusCode?: number;
        response?: unknown;
        validationError?: TypeValidationError;
        cause?: unknown;
        generationId?: string;
        isRetryable?: boolean;
    });
    static isInstance(error: unknown): error is GatewayResponseError;
}

declare const VERSION: string;

export { GATEWAY_AUTH_SUBPROTOCOL_PREFIX, GATEWAY_REALTIME_SUBPROTOCOL, GATEWAY_TEAM_SUBPROTOCOL_PREFIX, GATEWAY_TRANSCRIPTION_SUBPROTOCOL, type GatewayAsyncJobMetadata, GatewayAuthenticationError, type GatewayCreditsResponse, type GatewayEmbeddingModelId, GatewayError, type GatewayErrorResponse, type GatewayEvaluationModelId, GatewayFailedDependencyError, GatewayForbiddenError, type GatewayGenerationInfo, type GatewayGenerationInfoParams, type GatewayImageModelId, GatewayInternalServerError, GatewayInvalidRequestError, type GatewayLanguageModelEntry, type GatewayProviderOptions as GatewayLanguageModelOptions, type GatewayLanguageModelSpecification, type GatewayLanguageModelEntry as GatewayModelEntry, type GatewayModelId, GatewayModelNotFoundError, GatewayNotFoundError, type GatewayProvider, type GatewayProviderMetadata, type GatewayProviderOptions, type GatewayProviderSettings, GatewayRateLimitError, type GatewayRealtimeModelId, type GatewayRerankingModelId, GatewayResponseError, type GatewaySpeechModelId, type GatewaySpendReportParams, type GatewaySpendReportResponse, type GatewaySpendReportRow, type GatewayTranscriptionModelId, type GatewayVideoModelId, VERSION, createGateway, createGateway as createGatewayProvider, gateway, getGatewayRealtimeAuthToken, getGatewayRealtimeProtocols, getGatewayRealtimeTeamIdOrSlug, getGatewayTranscriptionProtocols };
